#include <stdio.h>

/**
 * main - prints its name
 * @argc: argument count
 * @argv: argument
 *
 * Return : 0
 */

int main(int argc, char **argv)
{
	(void)argc;

	printf("%S\n", argv[0]);
	return (0);
}
