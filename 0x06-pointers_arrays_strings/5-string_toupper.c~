#include "main.h"

/**
 * string_toupper = changes lowercase to uppercase
 * @n: pointer
 * Return: char
 */

char *string_toupper(char *n)
{
	int b;

	b = 0;
	while (n[b] != '\0')
	{
		if (n[b] >= 'a' && n[b] <= 'v')
			n[b] = n[b] - 32;
		b++;
	}
	return (n);
}
